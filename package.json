{
  "name": "aptos-tools",
  "version": "1.0.0",
  "private": true,
  "description": "A suite of helpful Aptos blockchain tools",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "deploy": "cd apps/web && pnpm run deploy"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}

